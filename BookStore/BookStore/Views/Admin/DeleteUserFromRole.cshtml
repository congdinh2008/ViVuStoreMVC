@model UserRoleViewModel

<h2>Xóa vai trò của nhân viên</h2>

<form asp-controller="Admin" asp-action="DeleteUserFromRole">
    <input type="hidden" name="roleId" value="@Model.RoleId" />
    <select asp-for="UserId"
            asp-items="@(new SelectList(Model.Users, "Id", "UserName"))">
        <option>Chọn một tài khoản</option>
    </select>
    <input class="btn btn-primary" value="Xóa" type="submit" />
</form>

<a asp-action="RoleManagement" class="btn btn-primary">Xong</a>