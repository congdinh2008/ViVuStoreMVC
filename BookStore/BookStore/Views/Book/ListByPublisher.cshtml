@model BooksListByPublisherViewModel
@{
    ViewData["Title"] = $"{Model.CurrentPublisher} | ViVuStore.vn";
}

<h2>@ViewBag.CurrentPublisher</h2>

<div class="row">
    @foreach (Book book in Model.Books)
    {
        @await Html.PartialAsync("_BookOverviewSummary", book)
    }
</div>